# name: Bump version.json for build

# on:
#   push:
#     branches:
#       - main   # adjust if your default branch differs

# jobs:
#   bump:
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0   # ensures git log works in bump-version.js
#           persist-credentials: true

#       - name: Setup Node
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'

#       - name: Bump version.json
#         run: node bump-version.js

#       - name: Commit version bump
#         run: |
#           git config --global user.name "github-actions[bot]"
#           git config --global user.email "github-actions[bot]@users.noreply.github.com"
#           git add client/src/version.json
#           git commit -m "chore: bump version [skip ci]" || echo "No changes to commit"
#           git push

#       - name: Build
#         run: |
#           unset CI
#           npm run build
name: Bump version.json for build

on:
  push:
    branches:
      - main  
    paths-ignore:
      - 'client/src/version.json'

jobs:
  bump:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Bump version.json
        run: node bump-version.js

      - name: Commit version bump
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add client/src/version.json
          git commit -m "chore: bump version [skip ci]" || echo "No changes to commit"
          git push
